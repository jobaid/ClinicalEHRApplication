# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Firebase
.env
serviceAccountKey.json
*firebase-adminsdk*.json
firebase-debug.log
firestore-debug.log
ui-debug.log
.firebase/
.tools/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.scratch/
tools/
migration-export/
server/medbill-server.exe
server/firebase-hash-config.json

# ---------- Secrets (added when preparing this repo to be public) ----------
# Broadened from a bare `.env` so a variant filename cannot slip through: .env.local,
# .env.production, .env.docker and anything else are all ignored, while the committed
# *.example templates stay tracked.
.env
.env.*
!.env.example
!.env.docker.example
!.env.firebase.example

# Service-account and private-key material, wherever it lands in the tree.
**/serviceAccountKey.json
**/*serviceAccountKey*.json
**/*firebase-adminsdk*.json
**/*-adminsdk-*.json
*.pem
*.key
*.p12
*.pfx
*.jks
*.keystore
credentials.json
client_secret*.json

# Firebase scrypt parameters captured during migration — describes how to verify real password
# hashes, so it is not something to publish.
server/firebase-hash-config.json
**/firebase-hash-config.json

# Development-only JWT signing secret, generated on first run when JWT_SECRET is unset. It
# signs sessions and derives the MFA secret-encryption key on this machine, so it is a real
# credential even though it never leaves a developer's laptop. Production refuses to start
# without a real JWT_SECRET, so this file has no counterpart there.
server/dev-jwt-secret
**/dev-jwt-secret

# Build output
server/medbill-server
server/medbill-server.exe
server/medbill-server.exe~

# Test artifacts
test-results/
playwright-report/
blob-report/

# ---------- Clinical EHR Manager (desktop control centre) ----------
# Electron runtime and packaged output: large, machine-specific, and rebuilt by
# `npm install` / `npm run dist` inside manager/.
manager/node_modules/
manager/dist/
# Generated from scripts/make-icons.mjs; keep icon.ico and the tray images, which
# the packaged build needs and which are small.
manager/build/icon-*.png

# Database dumps. These contain live patient data (names, DOB, SSN, insurance IDs) in the
# clear — they must never reach a repository, public or private.
*.dump
*.sql.gz
*.backup

# Plain-SQL database dumps also contain live patient data.
medbill.sql
